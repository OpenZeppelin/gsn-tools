import React from 'react'

import Layout from './Layout'


class Root extends React.Component {
    render = () => {
        return (
            <React.Fragment>
                <Layout/>
            </React.Fragment>

        )
    }
}

export default Root
